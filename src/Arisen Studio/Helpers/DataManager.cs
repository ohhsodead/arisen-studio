using ArisenStudio.Forms.Windows;

namespace ArisenStudio.Helpers
{
    public static class DataManager
    {
        public static void LoadDownloads(MainWindow window)
        {
            // TODO: Move the business logic from MainWindow.LoadDownloads here.
            // Use 'window' to access UI or state as needed.
        }
        // Add other data-related methods (SearchDownloads, LoadInstalledMods, etc.)
    }
}
